# Salin file ini menjadi .env lalu isi dengan nilai yang sebenarnya.
# Nilai URI bisa disalin langsung dari TRIP_BE/.env

# MongoDB connection string (wajib)
URI=mongodb+srv://username:password@cluster.mongodb.net/trip-app?retryWrites=true&w=majority
